import { DisplayCheckPipe } from './display-check.pipe';

describe('DisplayCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
